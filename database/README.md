# قاعدة بيانات منظومة الإيواء السياحي TAS.LY

## 1. الهدف

يحتوي هذا المجلد على ملفات قاعدة البيانات الخاصة بمنظومة الإيواء السياحي الليبية.

---

## 2. الملفات الموجودة

| الملف | الوظيفة |
|---|---|
| schema.sql | إنشاء جداول قاعدة البيانات |
| seed-data.sql | إدخال البيانات الأولية |
| README.md | شرح طريقة استخدام ملفات قاعدة البيانات |

---

## 3. ترتيب التشغيل

يجب تشغيل الملفات بالترتيب التالي:

```text
1. schema.sql
2. seed-data.sql